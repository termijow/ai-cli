[tool.poetry]
name = "ai-cli-backend"
version = "0.1.0"
description = "Backend server for AI-CLI"

[tool.poetry.dependencies]
python = "^3.10"
fastapi = ">=0.109.0"
uvicorn = ">=0.27.0"
pdfplumber = ">=0.11.0"
python-docx = ">=1.1.0"
markdown = ">=3.5.0"

# AI Orders Microservice - HuggingFace & Transformers
huggingface-hub = ">=0.20.0"
transformers = ">=4.38.0"
torch = ">=2.2.0"
whisper = ">=1.14.0"
sentence-transformers = ">=2.3.0"
pydantic = ">=2.5.0"
pydantic-settings = ">=2.1.0"
